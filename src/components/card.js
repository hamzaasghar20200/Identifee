import { Card, CardTitle, CardText } from 'reactstrap';

export default function Box({ childen }) {
  return (
    <Card body>
      <CardTitle>My Stats</CardTitle>
      <CardText></CardText>
    </Card>
  );
}
